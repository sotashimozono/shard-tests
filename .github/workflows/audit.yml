name: Audit

# Deliberately has no `paths:` filter and is deliberately not a required check.
# A required check that is filtered out does not count as skipped — it never
# reports, and the pull request stays unmergeable forever. A weekly advisory can
# also turn this red through no change of ours, which must not block `main`.
on:
  pull_request:
  schedule:
    - cron: '17 4 * * 1'

permissions:
  contents: read
  issues: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
